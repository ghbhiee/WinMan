# WinMan 1.3.0

主题：**白名单应用的「单视图模式」**——把 Finder、Chrome 这类多窗口应用的每个窗口当作独立视图来管理，像 Windows 任务栏一样。

主要更新：

- **单视图模式白名单**（默认 Finder、Chrome，设置中可增删）：只有白名单应用有悬停预览和单窗口管理，其他应用完全保持 macOS 原生行为，鼠标悬停零干扰。
- **打开一个窗口不再带出同应用的其他窗口**：预览点击、Dock 点击恢复、⌥Tab 切换统一只把目标窗口置前（SkyLight 单窗口置前，AltTab 同款技术）。
- **修复点击 Dock 图标恢复/最小化到错误窗口**：最小化 A 后再点，回来的一定是 A；预览中最近激活的窗口标题显示为蓝色，即 Dock 点击的目标。
- **预览面板重做**：无整体背景，只有一排悬浮窗口卡片，标题在上，顺序固定，面板紧贴 Dock；悬停卡片即 Aero Peek——窗口临时置前、标题闪烁提示，点击保留，移开恢复原状。
- **Finder 不再依赖 System Events**：与其他应用同走 Accessibility 路径，**自动化权限彻底不再需要**，设置向导精简为两步。
- 预览更快（默认悬停延迟 0.5s，最低 0.2s），预览已显示时滑到相邻图标立即切换。
- 修复：从未手动碰过「点击切换」「悬停预览」开关的用户，改动其他设置后这两项会被静默关闭。

安装说明：

1. 下载 ZIP 或 DMG，将 `WinMan.app` 移到 `/Applications`。
2. 从 1.2.0 升级无需重新授权（签名身份不变）；从 1.1.0 升级需重新授权一次辅助功能。
3. 当前版本未经过 Developer ID 公证。如果 macOS 阻止首次打开，请在 Finder 中
   右键 `WinMan.app` 并选择“打开”。
4. 觉得最小化动画慢可把系统的最小化效果改为「缩放」：
   `defaults write com.apple.dock mineffect -string scale && killall Dock`

校验文件见 `SHA256SUMS.txt`。完整变更列表见 `CHANGELOG.md`。
