# WinMan 1.2.0

这是一个大版本更新，围绕稳定性、能耗和「Windows 肌肉记忆」新增多项能力。

主要更新：

- **⌥Tab 全局窗口切换器**：跨应用按最近使用顺序列出所有窗口（含最小化，带角标）。
  按住 `⌥` 连续 `Tab` 循环、`⇧⌥Tab` 反向、松开 `⌥` 切换、`Esc` 取消，可在设置中关闭。
- **预览缩略图关闭按钮**：悬停缩略图右上角出现 ✕，点击直接关闭该窗口（应用的保存
  确认弹窗仍会正常出现）；新增「单窗口也显示预览」开关。
- **设置向导**：首次启动引导三项权限授权，状态实时刷新，菜单栏可随时重开。
- **界面中英双语**：跟随系统语言自动切换。
- **Dock 读取改为直接 Accessibility API**：不再经过 System Events，自动化权限现在
  仅 Finder 功能需要；应用匹配改用 bundle ID，任何系统语言下都精确。
- **窗口焦点事件驱动追踪**：修复用 `Cmd-\`` 等键盘方式切换窗口后，点击 Dock 图标
  操作到错误窗口的问题。
- **性能与稳定性**：AX 调用全局超时（无响应应用不再拖慢鼠标）、缩略图后台截取、
  远离 Dock 时大幅降低刷新频率、消除重复的跨进程调用。

安装说明：

1. 下载 ZIP 或 DMG，将 `WinMan.app` 移到 `/Applications`。
2. 本版本签名身份有变化：**从 1.1.0 升级需要重新授权一次**辅助功能（在系统设置的
   辅助功能列表中先移除或取消勾选旧的 WinMan，再重新添加/勾选）。首次启动的设置
   向导会引导完成。
3. 当前版本未经过 Developer ID 公证。如果 macOS 阻止首次打开，请在 Finder 中
   右键 `WinMan.app` 并选择“打开”。

校验文件见 `SHA256SUMS.txt`。完整变更列表见 `CHANGELOG.md`。
